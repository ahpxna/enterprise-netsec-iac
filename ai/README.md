# AI plane

The AI plane consumes versioned feature vectors and produces scored evidence.
It owns no firewall or endpoint-response credentials. Isolation Forest is the
MVP; sequence models follow only after the baseline is reproducible.
