diff --git a/.github/workflows/netdevops-ci.yml b/.github/workflows/netdevops-ci.yml
index 052c16a..69e2039 100644
--- a/.github/workflows/netdevops-ci.yml
+++ b/.github/workflows/netdevops-ci.yml
@@ -36,7 +36,14 @@ jobs:
         run: pytest tests/unit -q
 
       - name: Validate Docker Compose model
-        run: docker compose --profile siem --profile ids --profile ztna --profile dmz config --quiet
+        run: >-
+          docker compose
+          --env-file .env.example
+          --profile siem
+          --profile ids
+          --profile ztna
+          --profile dmz
+          config --quiet
 
       - name: yamllint
         run: yamllint .
@@ -83,8 +90,8 @@ jobs:
       - uses: actions/checkout@v7
       - uses: hashicorp/setup-terraform@v4
       - run: terraform -chdir=terraform/libvirt init -backend=false
-      - run: terraform -chdir=terraform/libvirt fmt -check -recursive
+      - run: terraform -chdir=terraform/libvirt fmt -check -diff -recursive
       - run: terraform -chdir=terraform/libvirt validate
       - run: terraform -chdir=terraform/vyos-fabric init -backend=false
-      - run: terraform -chdir=terraform/vyos-fabric fmt -check -recursive
+      - run: terraform -chdir=terraform/vyos-fabric fmt -check -diff -recursive
       - run: terraform -chdir=terraform/vyos-fabric validate
diff --git a/terraform/vyos-fabric/main.tf b/terraform/vyos-fabric/main.tf
index 59f0ff1..beb1856 100644
--- a/terraform/vyos-fabric/main.tf
+++ b/terraform/vyos-fabric/main.tf
@@ -41,14 +41,14 @@ resource "libvirt_volume" "node_override_base" {
 }
 
 resource "libvirt_volume" "node_disk" {
-  for_each = var.nodes
-  name     = "${each.key}-disk.qcow2"
-  pool     = libvirt_pool.cxyz.name
+  for_each       = var.nodes
+  name           = "${each.key}-disk.qcow2"
+  pool           = libvirt_pool.cxyz.name
   base_volume_id = contains(keys(var.node_image_overrides), each.key) ? (
     libvirt_volume.node_override_base[each.key].id
-    ) : each.value.platform == "vyos" ? (
+  ) : each.value.platform == "vyos" ? (
     libvirt_volume.base.id
-    ) : (
+  ) : (
     libvirt_volume.linux_base.id
   )
   format = "qcow2"
@@ -57,9 +57,9 @@ resource "libvirt_volume" "node_disk" {
 # Cloud-init ISO per node. VyOS nodes receive config.boot; Linux endpoints
 # receive a normal cloud-config plus MAC-matched network-config v2 data.
 resource "libvirt_cloudinit_disk" "node_ci" {
-  for_each = var.nodes
-  name     = "${each.key}-cloudinit.iso"
-  pool     = libvirt_pool.cxyz.name
+  for_each  = var.nodes
+  name      = "${each.key}-cloudinit.iso"
+  pool      = libvirt_pool.cxyz.name
   user_data = each.value.platform == "vyos" ? templatefile(
     "${path.module}/cloud-init/user-data.tftpl",
     {
@@ -67,7 +67,7 @@ resource "libvirt_cloudinit_disk" "node_ci" {
       ssh_key     = var.ssh_public_key
       boot_config = local.vyos_boot_configs[each.key]
     }
-    ) : templatefile(
+  ) : templatefile(
     "${path.module}/cloud-init/linux-user-data.tftpl",
     {
       hostname = each.key
@@ -79,7 +79,7 @@ resource "libvirt_cloudinit_disk" "node_ci" {
   })
 
   network_config = each.value.platform == "linux" ? yamlencode({
-    version = 2
+    version   = 2
     ethernets = merge(
       {
         for nic in local.interface_plan[each.key] : nic.device => merge(
@@ -103,8 +103,8 @@ resource "libvirt_cloudinit_disk" "node_ci" {
           match = {
             macaddress = lower(local.management_plan[each.key].mac)
           }
-          "set-name" = local.management_plan[each.key].device
-          dhcp4      = true
+          "set-name"        = local.management_plan[each.key].device
+          dhcp4             = true
           "dhcp4-overrides" = {
             "use-routes" = false
           }
@@ -142,7 +142,7 @@ resource "libvirt_domain" "node" {
   # requested address for this stable MAC; VyOS and Linux both request DHCP
   # without accepting a management-plane default route.
   network_interface {
-    network_name   = libvirt_network.mgmt.name
+    network_name = libvirt_network.mgmt.name
     mac          = local.management_plan[each.key].mac
     hostname     = each.key
     # IP ownership belongs only to intent/fabric.yaml. Libvirt uses this
@@ -170,7 +170,7 @@ locals {
     for node_name, node in local.fabric_intent.nodes : node_name => [
       for attachment in node.attachments : {
         network_name = try(libvirt_network.data[attachment.link].name, null)
-        bridge = try(
+        bridge       = try(
           local.fabric_intent.links[attachment.link].kind == "external_bridge"
           ? local.fabric_intent.links[attachment.link].bridge
           : null,
@@ -277,7 +277,7 @@ output "node_mgmt_ips" {
 
 output "node_interface_plan" {
   description = "Stable guest device, MAC, network, and address mapping; management is always last"
-  value = {
+  value       = {
     for node_name in keys(var.nodes) : node_name => {
       data       = local.interface_plan[node_name]
       management = local.management_plan[node_name]
