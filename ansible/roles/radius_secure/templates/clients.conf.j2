# Managed by Ansible. Every client has its own generated secret.
{% for c in radius_secure_clients %}
client {{ c.name }} {
    ipaddr = {{ c.ip }}
    secret = {{ c.secret }}
    require_message_authenticator = yes
    nas_type = other
}
{% endfor %}
