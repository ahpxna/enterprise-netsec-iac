---
# VPN-01 is enforced and validated at the routed boundary plus the WireGuard
# service.  It deliberately does not reject a private DC SSH listener: SEG-01
# requires that service for management-path authorization testing.
