---
- name: Detection + remote-access controls
  hosts: servers
  roles:
    - role: suricata           # IDS sensor config
      tags: [ids]

- name: Ship device logs through the authenticated TLS relay
  hosts: routers:firewalls
  roles:
    - role: syslog_tls
      tags: [logging, siem]
