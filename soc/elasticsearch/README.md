# Elasticsearch

Reserved for TLS configuration, index templates, lifecycle policy, service
identities, and persistent SOC data. Elasticsearch must not be exposed directly
to untrusted or public networks.
